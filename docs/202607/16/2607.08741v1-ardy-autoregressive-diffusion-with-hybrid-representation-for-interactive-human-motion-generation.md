---
title: "ARDY: Autoregressive Diffusion with Hybrid Representation for Interactive Human Motion Generation"
title_zh: "ARDY: 基于混合表示的自回归扩散用于交互式人体运动生成"
authors: "Kaifeng Zhao, Mathis Petrovich, Haotian Zhang, Tingwu Wang, Siyu Tang, Davis Rempe"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08741v1"
tags: ["query:human-aigc"]
score: 8.0
evidence: 人形机器人运动生成
tldr: 现有交互式人体运动生成方法难以同时满足实时性和可控性，特别是对复杂文本语义和长程目标的处理。提出ARDY框架，采用混合表示（显式根特征与潜在身体嵌入）结合两阶段自回归扩散去噪器，支持在线文本提示和灵活运动学约束。在HumanML3D和Bones Rigplay数据集上，该方法生成高质量运动并精确遵循约束，通过动态文本、关键帧、路径跟踪等交互演示验证了实用性与泛化能力。ARDY弥合了离线精确控制与在线实时生成之间的鸿沟。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08741v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1852, \"height\": 678, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08741v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 839, \"height\": 470, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08741v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1788, \"height\": 526, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08741v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 893, \"height\": 564, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08741v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 892, \"height\": 348, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08741v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1794, \"height\": 1016, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08741v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1863, \"height\": 327, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08741v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1850, \"height\": 278, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08741v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1852, \"height\": 952, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08741v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 896, \"height\": 289, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08741v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 897, \"height\": 323, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08741v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 833, \"height\": 178, \"label\": \"Table\"}]"
motivation: 现有方法在实时交互中要么推理速度慢，要么牺牲可控性，无法处理复杂文本和长程运动学约束。
method: 提出ARDY，基于混合表示（根特征与潜在身体嵌入）和两阶段自回归扩散去噪器，支持可变上下文历史与长程约束条件。
result: 在HumanML3D和Bones Rigplay上生成高质量运动，精确遵循文本和运动学约束，通过实时交互演示验证了方法有效性。
conclusion: ARDY实现了在线可控的流式人体运动生成，平衡了实时性与控制精度，适用于动画、仿真和机器人等交互应用。
---

## 摘要
在交互式应用中实时生成逼真的3D人体运动是动画、模拟和人形机器人技术的关键。虽然最近的离线运动生成方法通过文本和运动学约束提供了精确控制，但它们缺乏交互式设置所需的推理速度。相比之下，现有的在线方法能够实现实时合成，但往往牺牲可控性，或者由于有限的上下文窗口而难以处理复杂的文本语义和长期目标。在这项工作中，我们提出了ARDY，一个流式生成框架，通过实现通过在线文本提示和灵活的运动学约束可控的高保真运动生成来弥合这一差距。ARDY采用了一种混合表示，将显式的根特征与潜在的身体嵌入相结合，平衡了精确轨迹控制和高效生成学习。我们提出了一个两阶段自回归变换器去噪器，具有可变的历史上下文，并支持基于灵活、长期运动学约束的条件生成。通过在大规模运动捕捉数据集上训练，并直接基于从真实姿态采样的文本标签和运动学约束进行条件生成，ARDY自然而然地学习了可控生成，支持在线提示和灵活的长期目标。在HumanML3D基准测试和大规模高保真Bones Rigplay数据集上的广泛评估证明了ARDY的高运动质量和约束遵守能力，验证了我们关键架构决策的有效性。最后，我们通过一个交互式演示展示了该方法的实际多功能性，该演示具有动态文本控制、多样化的关键帧姿态约束、路径跟踪以及通过鼠标和键盘进行的交互式运动控制。补充视频结果、代码和模型发布可在https://research.nvidia.com/labs/sil/projects/ardy/找到。

## Abstract
Generating realistic 3D human motions in real-time within interactive applications is key for animation, simulation, and humanoid robotics. While recent offline motion generation approaches offer precise control via text and kinematic constraints, they lack the inference speed required for interactive settings. Conversely, existing online methods enable real-time synthesis but often sacrifice controllability or struggle with complex text semantics and long-horizon goals due to limited context windows. In this work, we introduce ARDY, a streaming generation framework that bridges this gap by enabling high-fidelity motion generation controllable via online text prompts and flexible kinematic constraints. ARDY employs a hybrid representation that combines explicit root features with a latent body embedding, balancing precise trajectory control with efficient generative learning. We propose a two-stage autoregressive transformer denoiser that features variable history context and supports conditioning on flexible, long-horizon kinematic constraints. By training on a large-scale motion capture dataset and being directly conditioned on text labels and kinematic constraints sampled from ground truth poses, ARDY natively learns controllable generation that supports online prompting and flexible long-horizon goals. Extensive evaluations on the HumanML3D benchmark and the large-scale, high-fidelity Bones Rigplay dataset demonstrate ARDY's high motion quality and constraint adherence, validating the efficacy of our key architectural decisions. Finally, we demonstrate the method's practical versatility through an interactive demo featuring dynamic text control, diverse keyframe pose constraints, path following, and interactive locomotion control via mouse and keyboard. Supplementary video results, code, and model releases can be found at https://research.nvidia.com/labs/sil/projects/ardy/.

---

## 论文详细总结（自动生成）

## 论文详细总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

**研究动机**：在动画、仿真和人形机器人等交互式应用中，实时生成逼真的3D人体运动至关重要。然而，现有方法存在明显的权衡：
- **离线方法**（如MaskControl、Kimodo）通过文本和运动学约束提供精确控制，但推理速度慢，无法用于交互场景。
- **在线方法**（如A-MDM、DiP、MotionStreamer）能够实时合成，但往往牺牲可控性，尤其难以处理复杂文本语义和长期目标（限于短上下文窗口）。

**核心问题**：如何同时实现**实时性**和**高可控性**，支持在线文本提示、灵活的运动学约束（如轨迹、关键帧、关节位置/旋转）以及长期目标规划。

**整体含义**：论文提出ARDY框架，通过混合表示和两阶段自回归扩散模型，弥合离线精确控制与在线实时生成之间的鸿沟，为下一代入机交互、游戏角色、仿真、机器人控制等应用提供基础。

### 2. 论文提出的方法论：核心思想、关键技术细节

**核心思想**：将人体运动分解为**显式的全局根轨迹**和**潜在的身体嵌入**（通过可学习的tokenizer获得），利用自回归扩散模型逐窗口生成运动，同时通过掩码方式注入任意稀疏的长期约束。

**关键技术细节**：

1. **混合表示（Hybrid Representation）**
   - 每帧表示为 `x = (m_root, x_body)`，其中 `m_root` 包含全局根位置、朝向（5维），`x_body` 是**潜在身体特征**（由tokenizer压缩得到），替代显式的高维身体参数（关节旋转、位置、速度等）。
   - 优点：根轨迹显式保证精确控制（可被直接替换/覆盖），身体潜空间紧凑，利于高效生成学习。

2. **身体运动Token化（Body Motion Tokenizer）**
   - 采用**因果注意力变压器**编码器-解码器结构，将每 `P` 帧（默认4帧）拼成一个patch，压缩为潜在令牌。
   - 解码器使用**局部表示**（根速度、高度）来减少脚滑。
   - 使用**有限标量量化（FSQ）** 稳定训练，潜在维度128，量化等级64。

3. **自回归两阶段扩散去噪器（Two-stage Autoregressive Denoiser）**
   - **数学形式**：模型 `F` 预测当前窗口 `C` 个清洁混合令牌 `x_1:C^0`，条件为：
     - 扩散步数 `k`
     - 文本 `s`
     - 历史上下文 `x_(-H+1):0`（可变长度，最多8秒）
     - 未来约束 `g_1:(C+F)`（可超出当前窗口，最多10秒）
   - **两阶段设计**：
     - 第一阶段：**根Transformer** 预测显式根轨迹 `m_root^0`。
     - 第二阶段：**身体Transformer** 以第一阶段预测的根为条件，预测潜在身体令牌 `x_body^0`。
     - 两阶段在去噪循环中交替迭代，保证相互影响。
   - **约束注入**：根部分通过**掩码覆盖**直接替换；身体约束通过**串联约束特征与掩码**到输入令牌中；超出窗口的约束作为额外令牌输入。

4. **条件生成训练**：从真实姿态中采样约束（轨迹、关键帧、关节等），使用多任务损失（混合损失、解码损失、目标损失、一致性损失），采用分类器无关引导（10%概率丢弃）。

5. **推理策略**：滑动窗口自回归生成，带**延迟感知重规划机制**：使用缓冲区隐藏推理延迟，保证平滑切换。

### 3. 实验设计：数据集、Benchmark、对比方法

**数据集**：
- **HumanML3D**（公开基准）：约30小时运动数据，原始预处理保留关节旋转，便于实时动画。
- **Bones Rigplay**（大规模商业数据集）：约700小时高质量动作捕捉，150+参与者，27关节骨架，约315k训练/35k测试（按动作类型划分，测试集包含未见类别）。

**任务与评估**：
- **文本+约束生成**：测试多种约束类型（根轨迹、关键帧、关节位置/旋转、脚部接触）。
- **指标**：
  - 文本对齐：Top-3 R-precision
  - 运动质量：FID、脚滑比率/速度
  - 约束精度：关节位置/旋转误差
  - 实时性：延迟（ms）

**对比方法**：
- **离线方法**：MaskControl（有/无测试时优化）
- **在线方法**：DiP（基于CAMDM扩展，支持文本和关节约束，但窗口短）
- 另外对比了**自消融基线**：显式表示、单阶段架构、全局根条件解码器等。

**额外**：进行了**感知实验**（240次成对比较），让用户评价运动质量、语义对齐、目标精度。

### 4. 资源与算力

文中明确说明：
- **Tokenization训练**：单块 **NVIDIA A100-SXM4-80GB GPU**，4百万步，批量128，学习率2e-5，约单卡完成。
- **去噪器训练**：四块 **NVIDIA A100-SXM4-80GB GPU**，批量512，1百万步，学习率2e-5。
- **交互演示推理**：**RTX 4090 GPU**，4步扩散模型延迟33ms，10步模型63ms。

### 5. 实验数量与充分性

实验非常充分，覆盖多个层面：
1. **消融实验**（在Bones Rigplay上）：
   - 混合表示 vs 显式表示
   - 全局→局部根转换
   - 两阶段 vs 单阶段架构
2. **超参数分析**（Tab.3）：生成窗口大小（4/8/12/20/40帧）、扩散步数（1/2/3/4/10/100）、Token化patch size（1/4/8）、潜在容量（FSQ 16-32/64-32/64-128/64-256）、Tokenizer类型（AE/VAE/FSQ）
3. **与SOTA对比**（HumanML3D）：
   - 离线对比（Tab.4）：MaskControl
   - 在线对比（Tab.5）：DiP（短窗口和长窗口目标）
4. **感知研究**（Tab.6）：240人次，用户偏好统计。

**充分性和公平性**：
- 实验组数多，涵盖了主要设计选择和关键参数。
- 对比方法均采用官方实现或标准设置，公平。
- HumanML3D评估使用原始评估器（与官方一致），但我们的数据预处理保留了关节旋转（与原始不同），作者承认这可能对R-precision略有不利。
- Bones Rigplay上测试集按动作类型划分，评估泛化到未见类别，更严格。

### 6. 论文的主要结论与发现

1. **混合表示**显著优于纯显式表示：运动质量（FID降低）和约束精度（关节误差降低数倍）均大幅提升。
2. **两阶段架构**优于单阶段：尤其是在约束满足上（关节误差降低超50%）。
3. **全局→局部根转换**减少脚滑（从0.303降到0.250）。
4. **支持长程目标**是独特优势：DiP在超出窗口目标时误差剧增（17.64cm），而ARDY保持极低误差（2.92cm）。
5. **在线推理快**：4步模型33ms，10步模型63ms，满足实时要求。
6. **感知实验**显示用户压倒性地偏好ARDY（>64% vs <10%）。
7. 在HumanML3D上，ARDY在约束精度上显著优于MaskControl（原始输出4.15cm vs 46.18cm），且无需测试时优化。

### 7. 优点：方法或实验设计上的亮点

- **方法创新**：
  - 混合表示巧妙结合显式根（可精确控制）和潜在身体（紧凑高效）。
  - 两阶段自回归去噪在扩散循环中交替更新根和身体，实现互相引导。
  - 原生支持长程约束，无需额外优化或强化学习策略。
  - 延迟感知重规划机制允许使用较慢模型仍保持流畅。
- **实验设计**：
  - 使用大规模商业数据集进行消融，结论更可靠。
  - 覆盖约束种类全面（轨迹、关键帧、关节位置/旋转）。
  - 不仅对比数值指标，还做了感知实验，增强说服力。
- **工程实现**：
  - 提供交互式演示（支持鼠标、键盘、文本动态修改）。
  - 开源代码和模型（承诺）。

### 8. 不足与局限

- **推理效率**：虽然已快（33ms），但仍需多步扩散，可进一步利用一致性模型或蒸馏加速。
- **无物理建模**：纯粹的动力学模型，可能出现脚滑、抖动等非物理现象（作者指出未来方向引入物理）。
- **历史上下文使用**：显式使用所有过去帧作为上下文，对于超长序列（几分钟）可能低效，需要更高效的记忆机制。
- **数据依赖**：Bones Rigplay为私有数据集，限制了可复现性；HumanML3D上虽表现好，但对比方法可能已在更大数据上预训练。
- **约束类型有限**：当前未支持接触力、物体交互约束等，对于复杂人-物交互场景有局限。
- **文本理解**：依赖LLM2Vec编码器，长文本或多个子动作可能仍需改进（但已优于DiP等短上下文方法）。

（完）
