---
title: "WorldDiT: A Unified Diffusion Architecture for World and Action Modeling"
title_zh: WorldDiT：一种用于世界与动作建模的统一扩散架构
authors: "Sen Wang, R. Gnana Praveen, Bidhan Roy, Marcos Villagra"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.23909v1"
tags: ["query:human-aigc"]
score: 9.0
evidence: 世界与行动建模，扩散Transformer
tldr: 现有机器人策略常依赖大型预训练视觉-语言模型作为动作骨干，导致参数量庞大。本文提出WorldDiT，一种统一的扩散transformer架构，将动作生成与视觉世界建模耦合，无需大型VLM。在四个LIBERO仿真套件上，WorldDiT以次十亿参数达到帕累托前沿性能，与使用大型VLM的方法相当。该工作为机器人学习中的缩放研究提供了高效的基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23909v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1674, \"height\": 766, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1614, \"height\": 1192, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1683, \"height\": 669, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1685, \"height\": 670, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23909v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1730, \"height\": 1165, \"label\": \"Table\"}]"
motivation: 现有方法依赖大型预训练VLM作为动作骨干，参数量大且计算成本高。WorldDiT旨在通过统一扩散架构同时建模动作和世界，降低对大型VLM的依赖。
method: 采用扩散transformer，训练时生成连续动作块并预测未来相机帧的归一化RGB patch目标，实现动作生成与视觉世界建模的联合学习。
result: 在四个LIBERO仿真套件上，WorldDiT在总参数量和平均成功率上位于帕累托前沿，性能优于许多使用更大VLM骨干的方法。
conclusion: WorldDiT提供了一个次十亿参数的强基线，证明统一的扩散架构可替代大型预训练VLM作为机器人动作骨干。
---

## 摘要
许多最近的机器人策略通过使用大型预训练视觉语言模型（VLM）作为动作骨干来追求更强的控制。我们提出了WorldDiT，一种统一的扩散变换器架构，将动作生成与视觉世界建模相结合，无需大型预训练VLM动作骨干即可实现强性能。在训练过程中，单个扩散变换器生成连续的动作片段，并从未来相机帧中预测归一化的RGB块目标。在四个LIBERO仿真套件中，WorldDiT在报告所有四个套件的方法中，处于总模型参数和平均成功率的帕累托前沿。这些结果为未来的扩展研究提供了一个强大的十亿参数以下基线。

## Abstract
Many recent robot policies pursue stronger control by using large pretrained vision-language models (VLMs) as the action backbone. We introduce WorldDiT, a unified diffusion transformer architecture that couples action generation with visual world modeling and achieves strong performance without a large pretrained VLM action backbone. During training, a single diffusion transformer generates continuous action chunks and predicts normalized RGB patch targets from future camera frames. Across four LIBERO simulation suites, WorldDiT lies on the reported Pareto frontier for total model parameters and mean success among methods reporting all four suites. These results provide a strong sub-billion-parameter baseline for future scaling studies.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：当前许多机器人操纵策略依赖大型预训练视觉-语言模型（VLM）作为动作骨干，导致模型参数庞大，且难以分离架构与规模对性能的贡献。论文旨在探索是否可以在不使用大型VLM动作骨干的前提下，通过统一的扩散变换器架构同时学习动作生成与未来视觉预测，从而实现强控制性能。
- **整体含义**：WorldDiT提出一种次十亿参数的统一扩散架构，将动作生成与未来RGB补丁预测耦合，在LIBERO仿真套件上达到与使用大型VLM方法相当的帕累托前沿性能，为未来缩放研究提供了紧凑基线，有望降低机器人策略对大规模预训练模型的依赖。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：使用单个扩散变换器（Diffusion Transformer, DiT）骨干，在训练时同时生成连续动作块和预测未来相机帧的归一化RGB补丁，但推理时仅使用动作路径，丢弃RGB预测头。
- **关键技术细节**：
  - **多模态条件编码**：使用冻结的视觉编码器（MAE）和语言编码器（CLIP），以及可训练的机器人状态编码器，将历史观测（多视图RGB、机器人状态、语言指令）编码为条件令牌。
  - **动作与未来视觉目标**：将历史窗口分为3步观测上下文（C=3），预测7步动作块（H=7），并从未来第H帧的主相机和腕相机中选取64个均匀间隔的归一化RGB补丁（每相机64个，共128个），仅对最后一个时间槽施加损失。
  - **流匹配训练**：采用Flow Matching目标函数，对动作和RGB补丁分别施加噪声，预测流速度。总损失为动作损失与RGB补丁损失的加权和（权重0.1和0.001）。
  - **推理**：从高斯噪声初始化动作令牌，通过20步欧拉积分预测动作块，执行前3个动作后重规划，并使用时间集成。

### 3. 实验设计：使用了哪些数据集/场景、benchmark、对比方法

- **数据集与场景**：使用LIBERO机器人操纵基准，包含四个下游套件：`LIBERO Spatial`、`LIBERO Object`、`LIBERO Goal`、`LIBERO Long`（共500个任务），另外使用大型多任务`LIBERO 90`作为预训练数据。
- **基准**：在四个套件上评估成功率和平均成功率，并与其他24种方法对比，包括使用大型VLM骨干的方法（如π0.5、ACoT VLA、MMaDA VLA等）和未使用大型VLM的方法（如DreamVLA、FlowVLA、DiT Policy等）。
- **对比指标**：总参数量（含冻结模块）与平均成功率，绘制帕累托前沿。

### 4. 资源与算力

- **算力配置**：
  - 单节点8块RTX Pro 6000 GPU（每块显存≈48GB）。
  - 预训练：在`LIBERO 90`上训练30个epoch，每GPU批大小40，梯度累积2，有效批大小640？原文为“per-GPU batch size of 40 and gradient accumulation of 2”，但后续微调时提到有效批大小512（8GPU×32×2），预训练未明确有效批大小。
  - 微调：每GPU批大小32，梯度累积2，8GPU，有效批大小512。
  - 混合精度：bf16。
  - 未明确总训练时间，但可推测为数十小时量级。

### 5. 实验数量与充分性

- **实验数量**：
  - 四个套件各评估500个模拟回合（共2000回合）。但每个套件中有300回合用于阶段性检查点选择（非完全独立测试），因此报告的平均成功率94.9%并非无偏估计。
  - 对比方法结果取自原始论文，未复现（因权重与代码未完全公开）。
- **充分性**：
  - 缺乏消融实验（如移除RGB预测目标、不同损失权重等），未分析各组件的贡献。
  - 仅在单一仿真基准LIBERO上评估，缺乏真实机器人或更多样化仿真环境的验证。
  - 对比方法仅基于报告值，可能存在协议差异，但论文在帕累托计算中排除了因缺少所有套件均值的Seer。
  - 实验设计相对充分，但未展示随机种子重复或置信区间，结果稳定性未知。

### 6. 论文的主要结论与发现

- WorldDiT（3.99亿总参数，1.35亿可训练参数）在LIBERO四个套件上平均成功率达到94.9%，在参数量-成功率帕累托前沿上位于次十亿参数区域，性能优于许多使用更大VLM骨干的方法（如OpenVLA、π0等），且与顶尖方法（如ACoT VLA 98.5%等）差距不大但参数少得多。
- 证明统一的扩散变换器可以替代大型预训练VLM作为动作骨干，并维持强性能，为未来缩放研究提供了紧凑基线。

### 7. 优点

- **架构创新**：将动作生成与未来视觉预测通过单一的Flow Matching目标耦合，训练时共享骨干，推理时仅保留动作路径，简洁高效。
- **参数效率**：次十亿参数即可达到与十亿参数级VLA方法竞争的性能，降低了计算和存储成本。
- **无需大型VLM骨干**：避免了大规模预训练模型的依赖，使方法更易部署于资源受限平台。
- **基准贡献**：为LIBERO基准提供了公开可比较的基线，促进未来扩展研究。

### 8. 不足与局限

- **实验覆盖不足**：仅在单一仿真套件LIBERO上测试，缺乏真实机器人实验、其他仿真环境（如MetaWorld、CALVIN）或复杂动态任务的验证。
- **缺乏消融与鲁棒性分析**：未量化未来RGB预测目标对性能的具体贡献；未报告多次运行的标准差或置信区间；未测试不同架构规模下的缩放规律。
- **测试偏差**：报告的平均成功率为94.9%，但包含300回合/套件用于检查点选择，并非完全独立的测试集，可能高估实际泛化能力。
- **对比不公平**：与对比方法未在同一协议下复现，仅依赖原始论文报告值，可能存在评估细节差异（如动作重复次数、随机种子等）。
- **应用限制**：方法需完美观测和离线训练数据，未考虑部分观测或域迁移；动作空间仅针对LIBERO的7自由度机械臂，未验证其他构型。
- **理论分析缺失**：未解释为何联合视觉预测能改善动作生成，缺乏可解释性分析。

（完）
