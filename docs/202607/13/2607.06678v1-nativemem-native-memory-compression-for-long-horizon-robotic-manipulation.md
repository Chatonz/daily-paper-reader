---
title: "NativeMEM: Native Memory Compression for Long-Horizon Robotic Manipulation"
title_zh: NativeMEM：面向长时域机器人操作的本地内存压缩
authors: "Ziye Wang, Modi Shi, Chaojun Ni, Jiazhi Yang, Mengdi Li, Zhizhong Su, Tianwei Lin, Hongyang Li"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06678v1"
tags: ["query:vla-humanoid"]
score: 9.0
evidence: 面向长时机器人操作的VLA模型记忆压缩
tldr: "现有VLA模型处理长历史视觉信息时效率低下，依赖外部记忆管理限制了记忆范围或响应能力。本文提出NativeMEM，通过原生记忆压缩方案，复用VLA视觉编码器将每帧历史压缩为单token，使预训练VLA能关注长期历史且延迟开销极低。仿真成功率从32.4%提升至84.0%，真实机器人达98.7%，同时保持低延迟和低GPU内存。贡献在于实现了长期实时更新的记忆，数据高效，仅用20%训练数据即达先进水平。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06678v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1440, \"height\": 585, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06678v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1370, \"height\": 612, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06678v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1401, \"height\": 581, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06678v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 843, \"height\": 425, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06678v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1435, \"height\": 415, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06678v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 636, \"height\": 384, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06678v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1456, \"height\": 410, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06678v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1450, \"height\": 273, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06678v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1453, \"height\": 283, \"label\": \"Table\"}]"
motivation: 现有VLA模型依赖外部记忆管理，限制了记忆长度或实时响应能力，难以同时兼顾长期历史与高频更新。
method: Native Memory Compression利用VLA视觉编码器将每帧历史压缩为单token，通过两阶段训练（先冻VLA训练记忆tokenizer，再微调VLA）对齐记忆与策略。
result: "仿真成功率从32.4%升至84.0%，真实机器人达98.7%，推理延迟和GPU内存占用低，且仅用20%数据即超越先前方法。"
conclusion: NativeMEM实现无需外部模块的长期高频记忆，显著提升长程操控性能，同时保持高效和数据经济性。
---

## 摘要
预训练的视觉-语言-动作（VLA）模型如何在不牺牲效率的前提下，以高频更新保留长时域的视觉历史？现有方法依赖外部内存管理，这限制了预训练策略的内存范围或响应速度。为此，我们提出NativeMEM，一种具有长期实时更新内存的VLA策略。其核心是一种高效的内存编码方案——本地内存压缩（Native Memory Compression），该方案复用VLA自身的视觉编码器，将每个摄像头视角的每一帧历史图像压缩为单个token。这些内存token附加到输入序列后，使得预训练的VLA能够以可忽略的延迟开销关注长期历史，无需外部规划器或新初始化的内存模块。为使内存token与预训练策略对齐，我们首先在内存密集型数据上，利用冻结的VLA监督训练一个通用内存分词器，然后解冻VLA以进行特定任务的微调。NativeMEM持续优于先前方法，在仿真中将成功率从32.4%提升至84.0%，在真实机器人上高达98.7%，同时保持低推理延迟和GPU内存使用。值得注意的是，NativeMEM表现出高数据效率，仅使用20%的训练数据即可达到与先前技术相当的结果。

## Abstract
How can pretrained Vision-Language-Action (VLA) models retain long-horizon visual histories with high-frequency updates without sacrificing efficiency? Existing approaches rely on external memory management, which restrains either the memory horizon or the reactiveness of pretrained policies. To this end, we present NativeMEM, a VLA policy that features long-term and real-time updated memory. At its core is an efficient memory encoding scheme, Native Memory Compression, which repurposes the VLA's own vision encoder to compress each historical frame from each camera view into a single token. Appended to the input sequence, these memory tokens enable the pretrained VLA to attend over long-term history with negligible latency overhead, requiring neither an external planner nor a freshly initialized memory module. To align the memory tokens with the pretrained policy, we first develop a generic memory tokenizer under the supervision of a frozen VLA on memory-demanding data, and then unfreeze the VLA for task-specific fine-tuning. NativeMEM consistently outperforms prior methods, boosting success rates from 32.4% to 84.0% in simulation and up to 98.7% on real robots, while maintaining low inference latency and GPU memory usage. Notably, NativeMEM exhibits high data efficiency by achieving competitive results with prior arts using only 20% of the training data.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：预训练的视觉-语言-动作（VLA）模型在处理长时域操作任务时，传统方法仅依赖当前观测，缺乏对历史交互的显式记忆，导致在需要跟踪进度、计数、恢复失败等非马尔可夫场景下性能急剧下降。
- **现有范式局限**：
  - VLM驱动记忆（如MemER、Mem-0）：依赖外部高维语言模块，需昂贵标注，且无法编码细微视觉细节。
  - 外部记忆模块（如HAMLET、MEM-short）：引入新模块，增加架构复杂度，且面临“压缩困境”：压缩不足则成本高，过度压缩则丢失动作关键信息。
- **作者动机**：设计一种高效、紧凑、与预训练VLA原生兼容的记忆机制，使其能够以极低开销保留长时间、细粒度的视觉历史，同时保持实时响应能力。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想：Native Memory Compression（本地内存压缩）
- **基本思路**：复用VLA自身的视觉编码器，将每一帧-视角对的历史观测压缩为**单个token**（M=1），这些token作为原生视觉证据追加到VLA的原输入序列中，使预训练策略通过原有注意力机制直接访问长期历史。

### 关键技术细节
1. **记忆分词器（Memory Tokenizer）**：
   - 从预训练VLA视觉编码器初始化记忆编码器 \( E_{\text{mem}} \)。
   - 引入可学习的记忆查询token \( q_{\text{mem}} \)，通过自注意力聚合输入图像的patch token序列，输出一个摘要token \( \hat{q}_v^\tau \)。
   - 线性投影层 \( W_{\text{mem}} \) 映射到VLA token维度，得到最终内存token \( m_v^\tau \)。
2. **记忆序列构建**：
   - 多视角、多时间步的内存token与一个可学习的开始token \( b_{\text{mem}} \) 拼接，附加到原始VLA输入（当前观测、语言、本体感觉）之后。
3. **两阶段训练**：
   - **阶段1（对齐）**：冻结预训练VLA，仅训练记忆分支（编码器、查询token、投影层、开始token）。使用VLA原生的动作预测损失 \( L_{\text{act}} \)，在混合数据（标准操作+记忆依赖任务）上训练，使内存token学会提取动作相关的历史信息。
   - **阶段2（微调）**：冻结训练好的记忆分词器，微调VLA骨干、动作头和记忆投影层，在目标任务的少量演示数据上优化。记忆token可离线预计算并缓存，降低训练存储开销。
4. **实时推理**：
   - 记忆分词器独立于VLA运行，以高频率将新帧压缩为内存token并维护一个队列；VLA被查询时，将当前内存序列直接拼接至输入，与原始策略推理解耦。

## 3. 实验设计：数据集/场景、基准、对比方法

### 任务场景
- **仿真**：5个任务，包括RMBench三个任务（Swap Blocks, Put Back Block, Observe and Pickup）和两个按钮按压任务（Click Buttons, Click Buttons Hard）。其中Hard版本移除颜色区分，迫使依赖空间记忆。
- **真实世界**：3个任务——Click Buttons（按顺序按键且不重复）、Put Back Block（将木块放回原位）、Grocery Checkout Scanning（跟踪已扫描物品，该任务在阶段1训练中**未见**，用于测试泛化性）。每个任务含多个子阶段。

### 对比方法
- 基础VLA：π0.5、X-VLA。
- VLM驱动记忆：MemER、Mem-0。
- 外部记忆模块：HAMLET*、MEM-short*（均基于相同π0.5骨架复现，确保公平）。
- 额外基线：π0.5+RTC（时间动作连续性方法）。

### 评估指标
- 仿真：任务成功率（%）。
- 真实：各阶段累计成功率，最后阶段为整体成功率。

## 4. 资源与算力

- **明确提及**：真实机器人任务只需**100个任务特定演示**和**约5小时微调**即可将单帧VLA转换为记忆增强策略。
- **未明确说明**：阶段1训练的具体GPU型号、数量、总时长；训练混合数据的规模；仿真实验的算力配置。论文未提供详细资源清单。

## 5. 实验数量与充分性

- **实验总量**：
  - 仿真5个任务×多个基线，共约35组数值比较。
  - 真实3个任务×4个基线，共12组比较。
  - 消融实验：对比无阶段1、稀疏更新（0.5Hz）、不同历史窗口长度（2s、4s、6s），共6组。
  - 数据效率实验：10、25、50个演示下的成功率对比。
  - 注意力可视化分析：空间注意力（Figure 5）和动作注意力（Figure 6）。
- **充分性与公平性**：
  - 基线复现：HAMLET和MEM-short均基于相同π0.5骨架复现，并标注*，控制变量。
  - 掩蔽未见任务（Grocery Checkout Scanning），验证泛化能力。
  - 消融实验覆盖记忆压缩机制、时间覆盖范围、更新频率等关键因素。
  - **不足**：
    - 仅使用π0.5一个骨干网络，未验证在OpenVLA、RT-2等其他VLA架构上的迁移性。
    - 真实世界任务仅三类，且均涉及桌面操作，未涵盖更复杂的移动操作或动态环境。
    - 无统计分析（如多次运行方差），结果可能受单次种子影响。

## 6. 论文的主要结论与发现

- **性能提升**：NativeMEM在仿真中平均成功率从32.4%（Mem-0）提升至**84.0%**；在真实机器人上整体成功率从34.7%（π0.5+RTC）提升至**98.7%**。
- **效率优势**：支持最长5000帧历史（32GB内存内），在100ms实时延迟约束下可处理约200帧，相比HAMLET和MEM-short历史长度提升9~40倍。
- **数据高效**：仅用10个演示即达到60%平均成功率（Mem-0仅17%），50个演示时性能超越现有方法。
- **注意力分析**：内存token的空间注意力聚焦于操作相关对象（如木块、按钮、待抓取物品）；动作token的注意力集中于任务关键历史时刻（如按钮按下瞬间、木块拾起时刻）。

## 7. 优点：方法或实验设计上的亮点

- **创新性记忆范式**：首次提出“将历史压缩到VLA自身视觉token空间”，无需外部模块，完全复用预训练架构与权重，保持架构友好性。
- **极致压缩与对齐**：每帧仅需1个token，且通过动作监督学习，使压缩兼顾效率与动作相关性，避免重建监督的信息冗余。
- **两阶段训练策略**：先冻VLA学通用记忆，再微调学任务特化，有效破坏灾难性遗忘，并支持迁移到未见任务（如Grocery Checkout Scanning）。
- **高数据效率**：仅需少量演示即可适应新任务，提升实际部署可行性。
- **可视化分析**：通过空间注意力和动作注意力热图，提供模型行为可解释性，验证记忆确实捕获了任务相关历史。

## 8. 不足与局限

- **持久记忆能力有限**：本文设计针对分钟级别历史，无法支持小时/天级别的长期持续性记忆，需额外系统级基础设施。
- **训练监督单一**：仅使用动作损失，可能在高阶语义要求（如规划推理）时产生语义鸿沟；更复杂的任务可能暴露该限制。
- **实验覆盖不足**：
  - 仅基于π0.5单骨干，未验证在其他VLA（如OpenVLA、RT-2）上的泛化性。
  - 真实世界任务仅3个桌面操作场景，排除移动操作、多机器人协作等复杂场景。
  - 缺乏多次运行方差报告，结果可能存在随机性。
- **计算资源未透明**：未公开阶段1训练所需算力与数据规模，影响可复现性。
- **可能的应用限制**：内存tokenizer需要先期在混合数据上训练，若新任务与训练数据分布差异极大，可能需重新训练或微调分词器。

（完）
