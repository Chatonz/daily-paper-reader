---
title: Explicit Language Memory for Long-Horizon Planning in Vision-Language-Action Models
title_zh: 视觉-语言-动作模型中用于长时程规划的显式语言记忆
authors: "Houze Xu, Jizhong Li, Ziyi Ye"
date: 2026-08-05
pdf: "https://arxiv.org/pdf/2608.04765v1"
tags: ["query:vla-humanoid"]
score: 8.0
evidence: 带显式语言记忆的VLA用于长时程规划
tldr: 现有VLA模型在长时程任务中面临跨任务泛化弱、时序一致性差、误差累积和语义表征退化等问题。本文提出带显式语言记忆的分层VLA架构，将离散时序观测转译为带时序逻辑的文本记忆，由高层VLM递归更新子任务指令，低层VLA执行连续控制。在仿真与真实机器人上的实验表明，该方法显著提升长时程任务成功率与鲁棒性，并提供可解释的决策语义。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.04765v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1699, \"height\": 711, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.04765v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1542, \"height\": 715, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.04765v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1623, \"height\": 765, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.04765v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1597, \"height\": 1048, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.04765v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1330, \"height\": 211, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.04765v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1714, \"height\": 395, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.04765v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1249, \"height\": 210, \"label\": \"Table\"}]"
motivation: 现有VLA模型因专家示范稀疏和马尔可夫假设，难以在长时程任务中保持时序一致与跨任务泛化，且端到端微调弱化VLM语义能力。
method: 提出分层架构，高层VLM通过视觉问答范式将观测转为文本记忆并递归更新子任务，低层VLA基于子任务指令执行连续控制。
result: 在多个仿真环境和真实机器人平台上验证，显式语言记忆有效提高了长时程复杂任务的完成成功率与鲁棒性，同时提供可解释决策过程。
conclusion: 显式语言记忆为长时程VLA规划提供了有效的时序锚定与纠错机制，显著增强语义一致性和任务表现。
---

## 摘要
视觉-语言-动作（VLA）模型为连接视觉感知、语言理解和机器人控制提供了统一范式。然而，现有VLA模型在长时程任务中仍面临重大挑战：稀疏的专家演示限制了跨任务的组合泛化；长时程任务的非马尔可夫性质使得仅基于当前观测的策略难以维持时间一致性；有限的闭环纠错能力导致执行误差累积；端到端的动作微调可能削弱视觉-语言模型（VLM）骨干网络的高层语义表示。为解决这些问题，我们提出了一种带有显式语言记忆模块的分层长时程VLA架构。核心思想是将离散的时间观测转换为具有时间逻辑的连贯文本记忆序列。该系统被解耦为高层VLM和低层VLA：高层VLM通过视觉问答训练范式进行语义推理，而低层VLA在子任务指令和视觉观测的条件下执行精确的连续控制。高层VLM利用先前记忆作为上下文锚点，递归更新语言记忆和子任务指令，从而在长时程执行过程中实现持续的时间跟踪和动态修正。我们在多个仿真环境中评估了所提方法，并在真实机器人平台上进行了sim-to-real实验。结果表明，显式语言记忆提高了VLA模型在复杂长时程任务上的成功率和鲁棒性，同时为决策过程提供了可解释的语义说明。

## Abstract
Vision-language-action (VLA) models provide a unified paradigm for connecting visual perception, language understanding, and robotic control. However, existing VLA models still face major challenges in long-horizon tasks: sparse expert demonstrations constrain cross-task compositional generalization; the non-Markovian nature of long-horizon tasks makes it difficult for policies conditioned only on current observations to maintain temporal consistency; limited closed-loop error correction allows execution errors to accumulate; and end-to-end action fine-tuning may weaken the high-level semantic representations of vision-language model (VLM) backbones. To address these issues, we propose a hierarchical long-horizon VLA architecture with an explicit language-memory module. The central idea is to convert discrete temporal observations into a coherent textual memory sequence with temporal logic. The system is decoupled into a high-level VLM and a low-level VLA: the high-level VLM performs semantic reasoning through a visual question answering training paradigm, while the low-level VLA executes precise continuous control conditioned on subtask instructions and visual observations. The high-level VLM recursively updates both language memory and subtask instructions using the previous memory as a contextual anchor, enabling persistent temporal tracking and dynamic correction during long-horizon execution. We evaluate the proposed method in multiple simulation environments and conduct sim-to-real experiments on a real robotic platform. The results demonstrate that explicit language memory improves the success rate and robustness of VLA models on complex long-horizon tasks while providing an interpretable semantic account of the decision process.

---

## 论文详细总结（自动生成）

# 论文总结

## 1. 论文的核心问题与整体含义

**研究动机与背景：**

- 视觉-语言-动作（VLA）模型将视觉感知、语言理解与机器人控制统一到一个策略中，是具身智能的重要范式。
- 然而，现有VLA模型在**长时程任务**中面临四大核心挑战：
  1. **泛化受限**：稀疏的专家示范限制了跨任务的组合泛化能力。
  2. **非马尔可夫性**：长时程任务中，仅依赖当前观测无法判断任务阶段（如同一视觉场景可能对应“已抓取/未抓取”“已按压/未按压”等不同状态），导致策略跳过阶段、重复已完成动作或执行失效动作。
  3. **误差累积**：有限闭环纠错能力使得执行误差随时间不断累加。
  4. **语义退化**：端到端动作微调可能削弱VLM骨干的高层语义表示。
- 直接拼接全部历史帧作为输入会带来上下文长度和计算成本的不可控增长。
- **论文核心主张**：以**显式自然语言记忆**作为任务历史的紧凑载体，提供跨决策步的时序锚定，提升长时程规划的阶段一致性。

## 2. 论文提出的方法论

**核心思想：**

- 将长时程策略分解为**高层语义分支**（高VLM）与**低层动作分支**（低VLA）：
  - 高层VLM：基于全局指令、当前观测和先前语言记忆，递归更新记忆并输出子任务指令，负责“做什么、任务到什么阶段”。
  - 低层VLA：基于当前观测和子任务指令生成固定长度（H=30）的连续动作块，负责“如何执行”。
- 整体策略概率分解：

  \[
  \pi(a_{t:t+H}, s_t, m_t \mid o_t, m_{t-1}, l) = \pi_{LL}(a_{t:t+H} \mid o_t, s_t) \cdot \pi_{HL}(s_t, m_t \mid o_t, m_{t-1}, l)
  \]

**关键设计：**

- **递归语言记忆**：
  - 记忆以结构化自然语言维护“已完成里程碑（过去时）+ 决策相关信息（物体属性/状态/失败）+ 下一步意图”。
  - 采用**滚动压缩规则**：子任务完成前保留细节，完成后压缩为简洁过去时语句，将注意力转向下一子任务，从而保证记忆有界。
- **自动记忆监督管线**：
  - 从专家视频、技能标签和时间戳出发，用通用VLM（如Gemini 3.5 Flash API或本地Qwen3.5-9B）逐帧生成对齐的“子任务+语言记忆”训练目标，无需人工逐帧标注。
- **独立分支训练**：
  - 高层使用PaliGemma（自回归预测记忆和子任务）；低层沿π0.5的动作专家，用flow-matching目标训练。
  - 两个分支独立训练，避免单一目标中语义与控制的损失失衡。
- **闭环推理算法**：
  - 初始化为空记忆；循环执行“观测→更新(m_t, s_t)→生成动作块→执行→判断终止”。

## 3. 实验设计

**评测场景与基准（Benchmark）：**

| 场景 | 环境/平台 | 任务内容 | 阶段数 |
|------|-----------|----------|--------|
| BEHAVIOR-1K收音机任务 | 仿真（真实场景类别） | 找到收音机→拿起→按按钮→放回桌上 | 4阶段（MoveTo/PickUp/PressOn/PlaceOn） |
| Genie Sim 3.0单包分拣 | 仿真（Isaac Sim人形操作平台） | 跟随包裹→拿起→放入扫描区→条码朝上→再拿起→放入蓝色箱→返回 | 6阶段 |
| Genie Sim 3.0连续分拣 | 仿真 | 任意顺序分拣4个不同颜色包裹，每个包裹需条码朝上放入扫描区 | 4阶段（Upright-1~4） |
| 真实XLeRobot拾取放置 | 真实低成本双臂移动机器人 | 从桌上拾取积木并放到盘子上 | 4阶段（Follow/PickUp/MoveTo/Place） |

**对比方法：**

- 基线：原生π0.5（无显式语言记忆）。
- 提出方法：π0.5 + 显式语言记忆分层架构。

**评估指标：**

- 主要指标为**阶段级成功率**（Stage-wise Success Rate），按各阶段平均计算，能够更细粒度地暴露长序列中的失败位置。

## 4. 资源与算力

论文明确提到的硬件与训练配置如下：

- 训练：4张NVIDIA A100 GPU。
- 推理：1张NVIDIA RTX 5880 Ada Generation GPU。
- 训练步数：50,000步；批大小64；峰值学习率2.5×10⁻⁵；优化器AdamW（warmup + 余弦衰减）。
- 动作块长度：H=30。
- 高层PaliGemma和低层VLM骨干用LoRA适配；300M参数Gemma动作专家全量微调。
- **注意：论文未明确报告训练时长（小时数）** ，也未说明数据集的绝对规模与构成细节。

## 5. 实验数量与充分性

- 论文共报告了 **7组实验**（BEHAVIOR-1K×1、Genie Sim单包×1、Genie Sim连续×1，加上真实机器人的1组），涵盖了家庭仿真、人形操作仿真和真实机器人三类场景。
- **消融实验缺失**：未对语言记忆的具体组件（如记忆长度、更新频率、不同VLM标注模型等）进行消融分析。
- **统计充分性不足**：论文未报告不同随机种子下的方差或置信区间，改进的统计稳定性未得到验证；真实机器人实验仅25条训练轨迹、20次评估试验，样本量较小。
- **对比公平性**：基线（π0.5）与提出方法共享低层动作专家，差异集中在高层语义模块，比较相对公平；但论文未报告更多外部VLA基线（如带通用记忆的MemoryVLA/HAMLET）的对比。

## 6. 论文的主要结论与发现

- **关键实验结果**：
  - BEHAVIOR-1K收音机任务：平均成功率从**30.0%**提升至**40.0%**，PickUp阶段从10%提升到30%。
  - Genie Sim单包分拣：从**41.7%**提升至**63.9%**，Follow和PickUp-1翻倍，Inside-1从50%提升到83.3%。
  - Genie Sim连续分拣：从**31.3%**提升至**46.9%**，基线从未完成第3、4个包裹，而记忆模型在12.5%的评估中完成全部4个。
  - 真实XLeRobot：从**62.5%**提升至**66.3%**，提升幅度较小（任务阶段少，符合预期）。
- **阶段一致性**：定性日志显示，无记忆基线会在按下按钮前就提前“放置收音机”；带记忆模型则严格依据观测证据推进阶段，减少跳步。
- **语义规划与运动控制的边界**：PressOn成功率依然很低（10%），说明语言记忆解决“该做什么”而不是“如何做好接触丰富的动作”，两种能力互补。
- **上下文失败恢复**：真实机器人日志中，记忆模型能够记录抓取失败状态、重发“重新张开夹爪”子任务并重试，且无需单独的恢复控制器。
- **总体结论**：显式语言记忆是一个紧凑、可解释的路径，可显著提升VLA在长时程多阶段任务中的成功率与阶段一致性，为长时程规划提供了有效的时序锚定与纠错机制。

## 7. 优点

- **架构设计新颖且清晰**：将高层语义规划与低层连续控制解耦，两个分支各有独立优化目标，避免多任务损失失衡。
- **显式语言记忆具有可解释性**：决策过程中的记忆和子任务均为可读文本，便于人工检查、诊断失败原因（高层语义错误 vs 低层控制错误）。
- **滚动压缩机制精巧**：在保留决策关键信息的同时控制上下文长度，避免全历史拼接的计算爆炸。
- **自动标注管线务实高效**：利用通用VLM从专家视频+技能标签自动生成训练目标，降低人工标注成本，使方法可扩展到新任务。
- **实验覆盖多层面**：同时涵盖家庭仿真、人形操作仿真和真实物理机器人，且从阶段成功率维度细粒度评估，能定位失败位置。
- **失败恢复以自然方式融入**：不依赖专门的恢复控制器，而是通过文本状态的自然条件化实现上下文自适应的重试，具有扩展潜力。

## 8. 不足与局限

- **真实实验规模小**：仅在短时程拾取-放置任务上做了真实机器人验证（25条轨迹/20次试验），未证明在分钟级多物体、重复交互或环境变化下的真实任务中的有效性。
- **统计严谨性不足**：未报告随机种子方差或置信区间，无法确认改进的统计显著性。
- **未提供消融分析**：未分离高层VLM选择、记忆更新策略、标注模型等因素的各自贡献。
- **计算开销增加**：分层设计在每次决策前引入额外的高层VLM调用，增加端到端延迟和部署复杂性；未来若能统一为单骨干模型需解决语义与控制的平衡问题。
- **无法补偿低层控制能力**：对需要精确几何对齐或力度控制的动作（如PressOn）改善有限，说明语言记忆的边界明确存在。
- **恢复能力依赖数据**：可靠性恢复需要覆盖多种失败与纠正轨迹的示范数据，而当前数据集缺乏此类多样性。
- **模型信息缺失**：论文未报告训练时长、数据绝对规模等关键信息，影响复现和可比较性。

（完）
